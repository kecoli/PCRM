% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt.outputMvoWts.R
\name{opt.outputMeanVolWts}
\alias{opt.outputMeanVolWts}
\title{opt.outputMeanVolWts}
\usage{
opt.outputMeanVolWts(opt, returns, digits = NULL, names = NULL)
}
\arguments{
\item{opt}{%% ~~Describe \code{opt} here~~}

\item{returns}{%% ~~Describe \code{returns} here~~}

\item{digits}{%% ~~Describe \code{digits} here~~}

\item{names}{%% ~~Describe \code{names} here~~}
}
\value{
%% ~Describe the value returned %% If it is a LIST, use %%
}
\description{
%% ~~ A concise (1-5 lines) description of what the function does. ~~
}
\details{
%% ~~ If necessary, more details than the description above ~~
}
\note{
%% ~~further notes~~
}
\examples{

##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
function (opt, returns, digits = NULL, names = NULL) 
{
    wts = opt$weights
    sigmasq = as.numeric(t(wts) \%*\% var(returns) \%*\% wts)
    sigma = sqrt(sigmasq)
    mu.ret = apply(returns, 2, mean)
    mu = as.numeric(t(wts) \%*\% mu.ret)
    if (is.null(digits)) {
        output = wts
    }
    else {
        if (is.null(names)) {
            output = list(wts = wts)
            output = lapply(output, round, digits)
        }
        else {
            output = list(wts = wts)
            names(output) = names
            output = lapply(output, round, digits)
        }
    }
    output
  }
}
\references{
%% ~put references to the literature/web site here ~
}
\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\author{
%% ~~who you are~~
}
\keyword{~kwd1}
\keyword{~kwd2}
